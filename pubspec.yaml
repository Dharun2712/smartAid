name: sdg
description: "A new Flutter project."
publish_to: 'none'
version: 2.0.8+16

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  provider: ^6.1.1
  geolocator: ^10.1.0
  permission_handler: ^11.0.1
  google_maps_flutter: ^2.5.0
  sensors_plus: ^4.0.0
  socket_io_client: ^2.0.3+1
  fl_chart: ^0.66.0
  intl: ^0.19.0
  tflite_flutter: ^0.10.4
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.2
  multicast_dns: ^0.3.2+6
  flutter_local_notifications: ^17.0.0
  audioplayers: ^5.2.1
  vibration: ^2.0.0
  flutter_polyline_points: ^2.0.0
  image_picker: ^1.0.7
  # speech_to_text: ^6.1.1  # Temporarily disabled due to build issues

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/sounds/
